{
  "name": "esbuild_ts_template",
  "version": "0.1.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && node scripts/build.mjs",
    "build:prod": "tsc && node scripts/build_prod.mjs",
    "start": "tsc && node scripts/serve.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Brian Ellis",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "esbuild": "^0.19.8",
    "typescript": "^5.3.2"
  }
}
